services:
  tvi_monitor.check.tpl.test.check:
    public: true
    class: Tvi\MonitorBundle\Test\Check\TestCheck\Check
    calls:
      - method: setAdditionParams
        arguments:
          - "%%args%%"
    tags:
      - {name: tvi_monitor.check_plugin, alias: "test:check"}
