tvi_monitor:
  checks:
    disk_free:
      check:
        size: 10000
        path: ~ #default /
    disk_free(s):
      items:
        a:
          check:
            size: 10000
        b:
          check:
            size: 10000
            path: /tmp
