tvi_monitor:

  checks_search_paths: ["%kernel.root_dir%/../Test/Check/**"]
  checks:
    core:php_version:
      check:
        expectedVersion: "7.0"
        operator: ">"

    test:success:check:
      check:

    test:success:check(s):
      items:
        a:
          check:
        b:
          check:

