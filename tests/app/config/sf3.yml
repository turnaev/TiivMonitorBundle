imports:
  - "tvi_monitor.yml"

framework:
  router:
    resource: "%kernel.root_dir%/config/routing.yml"
  secret: "test"
  templating:
    engines: ["twig"]
  test: ~
  profiler:
    collect: false
