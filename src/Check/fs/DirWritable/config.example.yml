tvi_monitor:
  checks:
    dir_writable:
      # group: "group"
      # tags: ["tag"]
      check:
        path: "/"
    dir_writable(s):
      items:
        a:
          check:
            path: "/"
        b:
          check:
            path: ["/", "/tmp"]
