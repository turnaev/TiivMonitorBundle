tvi_monitor:
  checks:
    core:dir_readable:
      # group: "group"
      # tags: ["tag"]
      check:
        path: "/"
    core:dir_readable(s):
      items:
        a:
          check:
            path: "/"
        b:
          check:
            path: ["/", "/tmp"]
