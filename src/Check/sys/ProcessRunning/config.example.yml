tvi_monitor:
  checks:
    process_running:
      # group: "group"
      # tags: ["tag"]
      check:
        processNameOrPid: "php"
    process_running(s):
      items:
        a:
          check:
            processNameOrPid: "nginx"
        b:
          check:
            processNameOrPid: 1
