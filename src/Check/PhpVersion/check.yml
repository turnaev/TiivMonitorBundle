services:
  tvi_monitor.check.php_version:
    public: true
    class: Tvi\MonitorBundle\Check\PhpVersion\Check
    arguments:
      - "%%expectedVersion%%"
      - "%%operator%%"
    calls:
      - method: setAdditionParams
        arguments:
          - "%%args%%"
    tags:
      - {name: tvi_monitor.check}
