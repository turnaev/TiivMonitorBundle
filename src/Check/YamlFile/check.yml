services:
  tvi_monitor.check.tpl.yaml_file:
    public: true
    class: Tvi\MonitorBundle\Check\YamlFile\Check
    #arguments:
    #  - "%%expectedVersion%%"
    calls:
      - method: setAdditionParams
        arguments:
          - "%%args%%"
    tags:
      - {name: tvi_monitor.check, alias: yaml_file}
