services:
  tvi_monitor.check.tpl.class_exists:
    public: true
    class: Tvi\MonitorBundle\Check\ClassExists\Check
    arguments:
      - "%%classNames%%"
      - "%%autoload%%"
    calls:
      - method: setAdditionParams
        arguments:
          - "%%args%%"
    tags:
      - {name: tvi_monitor.check, alias: class_exists}
