services:
  tvi_monitor.check.tpl.json_file:
    public: true
    class: Tvi\MonitorBundle\Check\JsonFile\Check
    #arguments:
    #  - "%%expectedVersion%%"
    calls:
      - method: setAdditionParams
        arguments:
          - "%%args%%"
    tags:
      - {name: tvi_monitor.check, alias: json_file}
