services:
  tvi_monitor.check.tpl.json_file:
    public: true
    class: Tvi\MonitorBundle\Check\IniFile\Check
    arguments:
      - "%%files%%"
    calls:
      - method: setAdditionParams
        arguments:
          - "%%args%%"
    tags:
      - {name: tvi_monitor.check, alias: json_file}
