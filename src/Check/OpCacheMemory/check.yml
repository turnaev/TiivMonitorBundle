services:
  tvi_monitor.check.tpl.op_cache_memory:
    public: true
    class: Tvi\MonitorBundle\Check\OpCacheMemory\Check
    #arguments:
    #  - "%%expectedVersion%%"
    calls:
      - method: setAdditionParams
        arguments:
          - "%%args%%"
    tags:
      - {name: tvi_monitor.check, alias: op_cache_memory}
