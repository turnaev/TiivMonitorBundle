services:
  tvi_monitor.check.tpl.couch_d_b_check:
    public: true
    class: Tvi\MonitorBundle\Check\CouchDBCheck\Check
    #arguments:
    #  - "%%expectedVersion%%"
    calls:
      - method: setAdditionParams
        arguments:
          - "%%args%%"
    tags:
      - {name: tvi_monitor.check, alias: couch_d_b_check}
