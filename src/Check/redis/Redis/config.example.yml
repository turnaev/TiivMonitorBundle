tvi_monitor:
  checks:
    core:redis:
      # group: "group"
      # tags: ["tag"]
      check:
        # host: default 'localhost'
        # port: default 6379
        # auth:  default null
    core:redis(s):
      items:
        a:
          check:
        b:
          check:
