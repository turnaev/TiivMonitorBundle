services:
  tvi_monitor.check.tpl.example:
    public: true
    class: Tvi\MonitorBundle\Check\Example\Check
    #arguments:
    #  - "%%expectedVersion%%"
    calls:
      - method: setAdditionParams
        arguments:
          - "%%args%%"
    tags:
      - {name: tvi_monitor.check, alias: example}
