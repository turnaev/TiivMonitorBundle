services:
  tvi_monitor.check.tpl.mongo:
    public: true
    class: Tvi\MonitorBundle\Check\Mongo\Check
    #arguments:
    #  - "%%expectedVersion%%"
    calls:
      - method: setAdditionParams
        arguments:
          - "%%args%%"
    tags:
      - {name: tvi_monitor.check, alias: mongo}
