services:
  tvi_monitor.check.tpl.dir_writable:
    public: true
    class: Tvi\MonitorBundle\Check\DirWritable\Check
    arguments:
      - "%%path%%"
    calls:
      - method: setAdditionParams
        arguments:
          - "%%args%%"
    tags:
      - {name: tvi_monitor.check, alias: dir_writable}
