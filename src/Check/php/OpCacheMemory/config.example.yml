tvi_monitor:
  checks:
    core:op_cache_memory:
      # group: "group"
      # tags: ["tag"]
      check:
      # warningThreshold: 70
      # criticalThreshold: 90
    core:op_cache_memory(s):
      items:
        a:
          check:
            warningThreshold: 70
            criticalThreshold: 90
        b:
          check:
            warningThreshold: 70
            criticalThreshold: 90
