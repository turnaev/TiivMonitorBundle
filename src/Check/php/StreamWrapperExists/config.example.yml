tvi_monitor:
  checks:
    core:stream_wrapper_exists:
      # group: "group"
      # tags: ["tag"]
      check:
        wrappers: ["zlib", "bzip2", "zip"]
    core:stream_wrapper_exists(s):
      items:
        a:
          check:
            wrappers: ["zlib"]
        b:
          check:
            wrappers: "zlib"
