tvi_monitor:
  checks:
    extension_not_loaded:
      # group: "group"
      # tags: ["tag"]
      check:
        extensionName: ["xdebug"]
    extension_not_loaded(s):
      items:
        a:
          check:
            extensionName: ["test"]
        b:
          check:
            extensionName: "xdebug"
