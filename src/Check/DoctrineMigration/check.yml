services:
  tvi_monitor.check.tpl.doctrine_migration:
    public: true
    class: Tvi\MonitorBundle\Check\DoctrineMigration\Check
    #arguments:
    #  - "%%expectedVersion%%"
    calls:
      - method: setAdditionParams
        arguments:
          - "%%args%%"
    tags:
      - {name: tvi_monitor.check, alias: doctrine_migration}
