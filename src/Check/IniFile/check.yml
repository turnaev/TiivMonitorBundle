services:
  tvi_monitor.check.tpl.ini_file:
    public: true
    class: Tvi\MonitorBundle\Check\IniFile\Check
    #arguments:
    #  - "%%expectedVersion%%"
    calls:
      - method: setAdditionParams
        arguments:
          - "%%args%%"
    tags:
      - {name: tvi_monitor.check, alias: ini_file}
