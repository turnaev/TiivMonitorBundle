services:
  tvi_monitor.check.tpl.memcached:
    public: true
    class: Tvi\MonitorBundle\Check\Memcached\Check
    #arguments:
    #  - "%%expectedVersion%%"
    calls:
      - method: setAdditionParams
        arguments:
          - "%%args%%"
    tags:
      - {name: tvi_monitor.check, alias: memcached}
