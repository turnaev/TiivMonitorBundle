services:
  tvi_monitor.check.tpl.rabbit_m_q:
    public: true
    class: Tvi\MonitorBundle\Check\RabbitMQ\Check
    #arguments:
    #  - "%%expectedVersion%%"
    calls:
      - method: setAdditionParams
        arguments:
          - "%%args%%"
    tags:
      - {name: tvi_monitor.check, alias: rabbit_m_q}
