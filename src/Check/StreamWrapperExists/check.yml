services:
  tvi_monitor.check.tpl.stream_wrapper_exists:
    public: true
    class: Tvi\MonitorBundle\Check\StreamWrapperExists\Check
    arguments:
      - "%%wrappers%%"
    calls:
      - method: setAdditionParams
        arguments:
          - "%%args%%"
    tags:
      - {name: tvi_monitor.check, alias: stream_wrapper_exists}
