services:
  {{ SERVICE_REPFIX }}.check.tpl.{{ CHECK_SPACE|replace({':':'.'}) }}{{ CHECK_ALIAS }}:
    public: true
    class: {{ NAMESPACE }}\Check
    #arguments:
    #  - "%%expectedVersion%%"
    calls:
      - method: setAdditionParams
        arguments:
          - "%%args%%"
    tags:
      - {name: tvi_monitor.check_plugin, alias: "{{ CHECK_SPACE }}{{ CHECK_ALIAS }}"}
