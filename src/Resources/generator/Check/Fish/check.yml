services:
  tvi_monitor.check.tpl.fish:
    public: true
    class: Tvi\MonitorBundle\Check\Fish\Check
    #arguments:
    #  - "%%expectedVersion%%"
    calls:
      - method: setAdditionParams
        arguments:
          - "%%args%%"
    tags:
      - {name: tvi_monitor.check, alias: fish}
